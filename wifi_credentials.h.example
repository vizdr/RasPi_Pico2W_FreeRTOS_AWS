#pragma once

// Copy this file to wifi_credentials.h (which is gitignored) and fill in your
// actual network details. wifi_credentials.h is never committed.

#define WIFI_SSID     "your-network-name"
#define WIFI_PASSWORD "your-network-password"
